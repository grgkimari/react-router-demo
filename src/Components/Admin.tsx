import { Typography } from "@mui/material"

 const Admin = () => {
    return(
        <Typography variant="h3" sx={{marginTop : "4rem"}}>
            Admin user
        </Typography>
    )
 }

 export default Admin