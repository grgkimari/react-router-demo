import { Typography } from "@mui/material"

const UserDetails = () =>{
    return(
        <Typography sx={{
            marginTop : "4rem"
        }}>Details about user</Typography>
    )
}

export default UserDetails